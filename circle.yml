machine:
  java:
    version: oraclejdk8

deployment:
  develop:
    tag: /v[0-9]+(\.[0-9]+)*-SNAPSHOT/
    commands:
      -  mvn deploy
  release:
    tag: /v[0-9]+(\.[0-9]+)*/
    commands:
      -  mvn deploy
